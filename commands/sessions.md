Manage Claude Code sessions for the current project.

Run: `~/.claude/scripts/sessions.sh $ARGUMENTS`

Display the script output to the user. When output contains `AWAITING_CONFIRMATION`, ask the user to confirm before re-running with `--yes`.
